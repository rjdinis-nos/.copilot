name: Build Customizations

on:
  push:
    branches:
      - main
    paths:
      - 'customizations/**'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Install uv
        uses: astral-sh/setup-uv@v3
      
      - name: Run build script
        run: uv run build.py
      
      - name: Update documentation
        run: uv run update_docs.py
      
      - name: Check for changes
        id: check_changes
        run: |
          git diff --quiet customizations.yml README.md index.html && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT
      
      - name: Configure git
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
      
      - name: Commit and push changes
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git add customizations.yml README.md index.html
          git commit -m "Update customizations and documentation [skip ci]"
          git push
